#include <cufft.h>

void perform_fft(size_t fft_size) {
    // cufftDoubleComplex *in, *out;
    // cufftHandle plan;

    // // size_t N = 8192;

    // cudaMalloc((void**) &in, sizeof(cufftDoubleComplex) * fft_size);
}